// Centralized timing and limits to avoid scattered magic numbers.
export const FLOOR_AUTO_RELEASE_MS = 60_000; // 60 seconds auto-release safety
export const FLOOR_RELEASE_DELAY_MS = 2_000; // delay after speaking before release
export const TEXT_INPUT_MAX_LENGTH = 200;
export const DEFAULT_TIMEOUT_MS = 5_000;
